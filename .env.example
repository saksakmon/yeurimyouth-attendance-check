VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Browser clients must use the anon key only. Do not expose the service_role key here.
# Supabase Auth role bootstrap example:
# app_metadata.admin_role = super_admin | admin | leader
